extends _jade_includes/_layout

block title
  title= "Knockback.js Tutorial: Inspector"

block includes
  link(rel="stylesheet",href="stylesheets/tutorial.css")
  include _jade_includes/_include_knockback_scripts

block navbar
  include _jade_includes/layout_components/_page_navbar
  mixin pageNavbar('Tutorials', false)

block content
  section
    h1='Tutorial: Knockback Inspector'

    a(href='http://kmalakoff.github.com/knockback-inspector/')='Try the demo'

    h2='Overview'

    p="This tutorial demonstrates how to use Knockback to create a tree view of potentially recursive models."
    p="The application is built using "
      a(href='http://brunch.io/')="Brunch"
      span=" to give it a standardized application structure. Brunch uses Node.js and Node Package Manager (npm) so it works on Windows, Mac, and Linux."

    h2='Building a Scaffold Project'
    ol
      li
        span="Install "
        a(href='http://nodejs.org/')='Node.js'
      li
        span="Install "
        a(href='http://brunch.io/')='Brunch'
        pre: code.
          npm install -g brunch
      li
        span="Create a new project: "
        pre: code.
          brunch new knockback-inspector
      li
        span="Build the new project"
        pre: code.
          cd knockback-inspector
          brunch build
      li
        span="Open 'public/index.html' to confirm the project is working. You should see a well-formatted Brunch Welcome! page"
      li
        span="Now we'll clean out everything we don't need:"
        ul
          li
            span="Delete everything in the 'app/models' folder"
      li
        span="Now add Knockout.js and Knockback.js:"
        ul
          li
            span="Put the latest "
            a(href='http://knockoutjs.com/')='knockout.js'
            span=" in 'vendor/scripts'"
          li
            span="Put the latest "
            a(href='http://kmalakoff.github.com/knockback/')='knockback.js'
            span=" in 'vendor/scripts'"
          li
            span="Update config.coffee to point to the new files. it should look like:"
            pre: code.
              files:
                javascripts:
                  ...
                  order:
                    before: [
                      'vendor/scripts/console-helper.js',
                      'vendor/scripts/jquery-1.7.2.js',
                      'vendor/scripts/underscore-1.3.1.js',
                      'vendor/scripts/backbone-0.9.2.js',
                      'vendor/scripts/knockout-2.1.0.js',
                      'vendor/scripts/knockback-0.15.1.js'
                    ]
      li
        span="Build the project again and check that there are no errors in your console. Use 'brunch watch' to make sure all changes are automatically picked up"
        pre: code.
          brunch watch
      li
        span="Update the main Template and View to make sure Knockback.js is working"
        ul
          li
            span="Update 'app/views/templates' to:"
            pre: code.
              &lt;div id="content"&gt;
                &lt;h1 data-bind="text: title"&gt;&lt;/h1&gt;
              &lt;/div&gt;

          li
            span="Update 'app/views/home_view.coffee' to:"
            pre: code.
              module.exports = class HomeView extends View
                id: 'home-view'
                template: template

                constructor: ->
                  super

                  @view_model = kb.viewModel(new Backbone.Model({title: 'Knockback Inspector'}))

                afterRender: ->
                  ko.applyBindings(@view_model, @el)
          li
            span="Update the following selectors in 'app/styles/main.styl' to:"
            pre: code.
              #home-view
                width: 800px
                margin: 0 auto
                background-color: #F5E0FF

              h1
                text-align: center
                font-size: 2em
                color: #fff
                border-radius: 5px
                background-color: #CC66FF
          li
            span="Refresh your browser page and you should see meaning we're ready to go!"
            img(src='images/tutorial_inspector/application_setup.jpg')

    h2='Design Overview'
    p='TODO'

    h2='Step 1'
    p='TODO'

    h2='Step 2'
    p='TODO'
